import { MutationTree } from 'vuex'
import { CalcStateInterface } from './state'

const mutation: MutationTree<CalcStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
}

export default mutation
